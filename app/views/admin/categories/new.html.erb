<div class="container col-md-12">

  <div class="card">
    <div class="card-header">
      <strong class="card-title">Create Menu Category</strong>
      <strong class="card-actions"><%= link_to 'Back', admin_categories_path %></strong>

    </div>

    <%= render 'form', category: @category %>
  </div>
</div>
