<p id="notice"><%= notice %></p>

<div class="container bounceInRight">
  <div class="card">
    <div class="card-header">
      <strong class="card-title">Menu Items</strong>
      <strong class="card-actions">
        <%= link_to 'New Category', new_admin_category_path %>
      </strong>
    </div>
    <div class="card-body">
      <table class="table table-striped table-bordered">
        <thead>
        <tr>
          <th>Name</th>
          <th colspan="3"></th>
        </tr>
        </thead>

        <tbody>

        <% @categories.each do |category| %>
          <tr>
            <td><%= category.name %></td>
            <td><%= link_to 'Show', admin_category_path(category), class: 'btn btn-info btn-sm' %></td>
            <td><%= link_to 'Edit', edit_admin_category_path(category), class: 'btn btn-warning btn sm' %></td>
            <td><%= link_to 'Destroy', admin_categories_path(category), class: 'btn btn-danger btn sm' ,method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
<br>
