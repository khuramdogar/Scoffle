<div class="container col-md-12">

  <div class="card">
    <div class="card-header">
      <strong class="card-title">Edit Menu Items</strong>
      <strong class="card-actions"><%= link_to 'Back', admin_category_path(@item.category) %></strong>

    </div>

    <%= render 'form', item: @item %>
  </div>
</div>